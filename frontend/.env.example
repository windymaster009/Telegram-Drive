# Local web development API. Production builds default to same-origin /api via .env.production.
VITE_API_BASE_URL=http://localhost:14201

# Optional Google OAuth values used by the browser UI.
VITE_GOOGLE_OAUTH_CLIENT_ID=
VITE_GOOGLE_OAUTH_REDIRECT_URI=http://localhost:1420/auth/google/callback
